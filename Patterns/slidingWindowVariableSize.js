let left = 0;

for (let right = 0; right < arr.length; right++) {
  // include arr[right] in the window

  while (window is invalid) {
    // shrink window from the left
    left++;
  }

  // now window [left..right] is valid → update result
}
