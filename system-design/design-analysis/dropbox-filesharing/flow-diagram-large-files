 ┌──────────┐
 │  Client  │
 └──────────┘
      │
      ├───────────────────────────────────────────────────────────────┐
      │                                                               │
      ▼                                                               ▼
[UPLOAD PATH]                                                   [DELETE/RETENTION PATH]

 1. Start upload session                                       1. Delete request
    - AuthN/AuthZ with token                                      - User/admin deletes file/folder
    - Requests uploadSessionId                                    - Request goes to Metadata Service
      ▼                                                               ▼
 ┌─────────────────────────────────────────────┐             ┌──────────────────────────────┐
 │    Upload Gateway / Intelligence Layer      │             │      Metadata Service        │
 │─────────────────────────────────────────────│             │──────────────────────────────│
 │ 1. Session Management                       │             │ • Write tombstone flag       │
 │    - Issues uploadSessionId                 │             │ • Check retention/holds      │
 │    - Sets chunk size (e.g., 8–64 MB)        │             │ • Record audit log           │
 │    - Checks quotas, policies, rate limits   │             │ • Enqueue Outbox event       │
 │                                             │             └──────────────────────────────┘
 │ 2. Chunk Handling                           │                      │
 │    - Generates pre-signed URLs              │                      │ Delete event
 │    - Direct-to-object-store uploads         │                      ▼
 │    - Tracks session progress in Redis/DB    │             ┌──────────────────────────────┐
 │                                             │             │          Event Bus           │
 │ 3. Integrity & Resilience                   │             │──────────────────────────────│
 │    - Enforces checksum/hash validation      │             │ • Fans out delete events     │
 │    - Supports retry/resume per chunk        │             │ • Update search index        │
 │    - Detects anomalies (abuse/DLP hooks)    │             │ • Trigger compliance hooks   │
 │                                             │             │ • Notifies other devices     │
 │ 4. Commit Coordination                      │             └──────────────────────────────┘
 │    - Receives POST /upload/commit           │                      │
 │    - Forwards manifest details              │                      │ Async cleanup
 │      (chunks[], metadata) to Metadata Svc   │                      ▼
 └─────────────────────────────────────────────┘             ┌──────────────────────────────┐
      │                                                      │     Garbage Collector        │
      │ Parallel uploads (chunks via URLs)                   │──────────────────────────────│
      ▼                                                      │ • Scan chunk ref counts      │
 ┌──────────────────────────────┐                            │ • Respect retention/holds    │
 │       Object Storage         │                            │ • Delete unreferenced chunks│
 │ - Stores 8–64 MB chunks      │                            └──────────────────────────────┘
 │ - Verifies checksums         │
 │ - Durable (11 nines)         │
 └──────────────────────────────┘
      │
      │ Commit manifest OR Sharing update
      ▼
 ┌──────────────────────────────┐
 │      Metadata Service        │
 │──────────────────────────────│
 │ • Validate chunks exist      │
 │ • Create Manifest record     │
 │ • Create new Version         │
 │ • Update latestVersionId     │
 │ • For sharing: update ACLs   │
 │   / mint tokens / audit log  │
 │ • Enqueue Outbox event       │
 └──────────────────────────────┘
      │
      │ Event fanout
      ▼
 ┌──────────────────────────────┐
 │          Event Bus           │
 │──────────────────────────────│
 │ • Fans out file changes      │
 │ • Fans out sharing changes   │
 │ • Fans out delete events     │
 │ • Triggers indexing/previews │   ◄─── [SEARCH/INDEXING PATH starts here]
 │ • Sends webhooks/integrations│
 │ • Notifies other devices     │
 └──────────────────────────────┘
      │
      │ Async indexing pipeline
      ▼
 ┌──────────────────────────────┐
 │     Indexing Pipeline        │
 │──────────────────────────────│
 │ • Fetch chunks/manifest      │
 │ • Assemble file              │
 │ • OCR / text extraction      │
 │ • Metadata enrichment        │
 │ • Build search document      │
 └──────────────────────────────┘
      │
      ▼
 ┌──────────────────────────────┐
 │   Search Index (ES/OS)       │
 │ • Content + metadata stored  │
 │ • ACL filters applied        │
 └──────────────────────────────┘
      │
      │ User initiates search query
      ▼
 ┌──────────┐
 │  Client  │
 └──────────┘
      │
      │ Query → Search Service
      ▼
 ┌──────────────────────────────┐
 │     Search Service           │
 │──────────────────────────────│
 │ • Parses query               │
 │ • Enforces ACLs              │
 │ • Queries search index       │
 │ • Builds ranked results      │
 └──────────────────────────────┘
      │
      ▼
 ┌──────────────────────────────┐
 │    Results to Client         │
 │ • Titles, snippets, previews │
 │ • Only files user can access │
 └──────────────────────────────┘
