## 1. Push

```plaintext
push(n) {
    this.stack.push(n);
}
```

## 2. Pop

```plaintext
pop() {
    return this.stack.pop();
}
```

## 3. Peek

```plaintext
peek() {
    return this.stack[this.stack.length - 1];
}
```
